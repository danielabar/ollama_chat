<div class="w-full">
  <h1>Welcome#index</h1>

  <%# ubscribe to welcome channel for real-time updates via ActionCable %>
  <%= turbo_stream_from "welcome" %>

  <%# Here is where we will stream the responses from the llm %>
  <div id="messages"></div>

  <%# The user types in their prompt here %>
  <%= render "form" %>
</div>
